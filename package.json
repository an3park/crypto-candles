{
  "name": "vite_from_scratch",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "vite build && vite preview --host"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "vite": "^4.2.1"
  }
}
